<ul>
  <% blog.articles.each do |article| %>
    <li class="grid grid-cols-[1fr_4fr] gap-4">
      <time class="text-right">[<%= article.date.strftime('%b %e %Y') %>]</time>
      <%= link_to article.title, article.url %>
    </li>
  <% end %>
</ul>
